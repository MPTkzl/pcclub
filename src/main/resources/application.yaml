spring:
  config:
    name: shop_web
  datasource:
    url: jdbc:postgresql://localhost:5432/pc_club
    username: postgres
    password: 1231
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql:
server:
  port: 7654

